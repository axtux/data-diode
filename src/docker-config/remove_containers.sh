# Stop and remove all containers
sudo docker stop diode-in && sudo docker rm diode-in
sudo docker stop diode-out && sudo docker rm diode-out
sudo docker stop inside-machine && sudo docker rm inside-machine
sudo docker stop outside-machine && sudo docker rm outside-machine
